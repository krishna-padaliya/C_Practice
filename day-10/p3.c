#include<stdio.h>
void main()
{
	for (int i=1; i<=7; ++i)
	{
		for (int j=1; j<=7; ++j)
		{
			if(i==1 && (j>=1 && j<=3) || i==1 && (j>=5 && j<=7) ||
				i==2 && (j>=1 && j<=2) || i==2 && (j>=6 && j<=7)||
				i==3 && j==1 || i==3 && j==7 ||

				i==7 && (j>=1 && j<=3) || i==7 && (j>=5 && j<=7) ||
				i==6 && (j>=1 && j<=2) || i==6 && (j>=6 && j<=7)||
				i==5 && j==1 || i==5 && j==7)
			{
				printf("*");
			}
			else{
                 printf(" ");
			}
		}
		printf("\n");
	}
}